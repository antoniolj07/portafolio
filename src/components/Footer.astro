---
import { getDictionary, getLangFromPath } from "@/i18n";

const lang = getLangFromPath(Astro.url.pathname);
const dict = getDictionary(lang);
---

<footer class="container py-4 text-center">
  <p>{dict.site.footer}</p>
</footer>
